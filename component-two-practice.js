"use strict";
const { COMPONENT_TWO_TOPICS, VERSION } = require("./component-two");

const drafts = [
  ["2.1.1", "model-devise", "Explain", "A campus app models accessible walking routes. Explain three details its model should retain and one detail it could omit.", ["Retain usable paths between locations.", "Retain accessibility restrictions, such as steps or ramps.", "Retain a route cost such as distance or expected time.", "Omit an irrelevant visual detail, such as the colour of nearby walls, and justify why it does not affect this route choice."]],
  ["2.1.1", "model-limits", "Evaluate", "A flood simulation ignores changes in ground absorption. Evaluate this abstraction when comparing heavy-rain scenarios.", ["The omission simplifies the model and may reduce computational work.", "Absorption affects how much rainfall becomes surface water.", "Ignoring variation can therefore misrepresent flooding in some scenarios.", "Judge suitability against the required accuracy and validate against observed data."]],
  ["2.1.2", "cache-stale", "Explain", "A revision app caches a student's quiz results. Explain a benefit, a risk and a suitable response to that risk.", ["Cached results can be shown without repeatedly fetching or recalculating them.", "The copy can become stale after a new attempt or a result correction.", "Invalidate or refresh the cache when results change, with an appropriate expiry policy."]],
  ["2.1.2", "document-contract", "Describe", "Describe the input data, a precondition and the returned output for a function that calculates a mean from an array of marks.", ["Specify the input as a collection of numeric marks.", "Require a non-empty collection, or explicitly define empty-input handling.", "Specify the returned numeric mean as total divided by count."]],
  ["2.1.3", "problem-parts", "Design", "Decompose an online club-registration process into sub-procedures and explain a necessary ordering dependency.", ["Identify a routine for collecting and validating registration details.", "Identify a routine for checking availability or eligibility.", "Identify a routine for storing a confirmed registration and reporting its outcome.", "Explain why required checks must succeed before a registration is confirmed."]],
  ["2.1.3", "independent-testing", "Identify", "An application imports marks and displays their mean. Identify two suitable sub-procedures and explain one testing benefit of this decomposition.", ["validateMark checks whether each input is a numeric mark in the allowed range.", "calculateMean totals accepted marks and divides by their count, handling empty input.", "Each routine can be tested separately with known inputs and expected outputs before integration testing."]],
  ["2.1.4", "boundary-test", "Explain", "A valid mark is from 0 to 100 inclusive. A routine accepts a mark when mark > 0 OR mark < 100. Explain why the condition accepts invalid values, provide a corrected condition and give boundary and out-of-range test values.", ["OR allows invalid values to satisfy just one side.", "The corrected bounds must include equality; the original OR condition actually accepts every numeric value, including 0 and 100.", "Use mark >= 0 AND mark <= 100.", "Test the boundaries 0 and 100 and out-of-range values such as -1 and 101."]],
  ["2.1.4", "independent-if", "Compare", "A rewards routine has separate IF score >= 50 and IF score >= 80 statements. Compare its behaviour at score 90 with an IF / ELSE IF chain in that order, then describe a change that selects only the highest eligible reward.", ["Both independent conditions are true, so both bodies can run.", "In the chain, the first branch matches and later branches are skipped.", "A high-threshold-first chain may be needed if only the highest reward should be chosen."]],
  ["2.1.5", "race", "Explain", "Two requests attempt to reserve the final bicycle in a hire station. Explain how concurrency can cause an error and how coordination can prevent it.", ["Both requests can read the same available state.", "Both might then confirm the same bicycle before seeing the other's update.", "Make the availability check and reservation update atomic, for example within a suitable transaction or lock.", "Handle the losing request with an unavailable result and release locks appropriately."]],
  ["2.1.5", "coordination", "Evaluate", "A program processes 100 independent images. Evaluate using multiple workers instead of one.", ["Independent image work can be distributed across workers.", "Parallel hardware may reduce elapsed processing time.", "Coordination, memory and input/output contention add costs.", "Measure the workload and choose a bounded worker count rather than assuming more workers always help."]],
  ["2.2.1", "recursion", "Trace", "A function sumTo(n) returns 0 when n = 0 and otherwise returns n + sumTo(n-1). Trace sumTo(3) and explain the base case.", ["Calls descend through 3, 2, 1 and 0.", "At 0 the function returns 0 without another recursive call.", "The returns unwind to 1, then 3, then 6.", "Decreasing n reaches the base case for non-negative integer inputs."]],
  ["2.2.1", "encapsulation", "Explain", "A game has Player and Enemy objects with health. Explain how encapsulation and polymorphism could be used for taking damage.", ["Keep health state behind a controlled object interface.", "A damage method can enforce valid health updates.", "Both object types can expose the same method interface.", "Each type can implement its own damage behaviour when that method is called."]],
  ["2.2.2", "heuristic", "Evaluate", "A delivery service needs a route quickly through many stops. Evaluate a heuristic solution instead of enumerating every route.", ["The number of possible routes can make exhaustive enumeration impractical.", "A heuristic uses a rule or estimate to find a useful route more quickly.", "It need not find the globally shortest route.", "Judge route quality against the time available and the service's operational constraints."]],
  ["2.2.2", "pipeline-limit", "Explain", "A processing pipeline reads a file, transforms its data and writes results. Explain a potential performance benefit and two limitations.", ["Stages can overlap on different items, increasing throughput.", "The slowest stage limits sustained throughput.", "Dependencies, buffering or coordination add costs; the latency of an individual item need not fall."]],
  ["2.3.1", "binary-debug", "Explain", "An ascending binary search uses inclusive low and high bounds. When a[mid] < target it sets high = mid - 1. Explain the error, give the correct update and state when the search should stop with a not-found result.", ["Values below or at mid are too small.", "The target can only remain above mid if it exists.", "Set low = mid + 1, leaving high unchanged.", "Continue while low <= high and return not found when the interval is empty."]],
  ["2.3.1", "merge-complexity", "Compare", "Compare conventional array merge sort and quicksort for a large list, including time and extra space.", ["Merge sort has O(n log n) worst-case time.", "Quicksort typically has O(n log n) average time but can reach O(n squared) with badly unbalanced partitions.", "Conventional array merge sort uses O(n) auxiliary storage.", "In-place quicksort needs stack space, commonly O(log n) for balanced partitions and O(n) in a naive worst case."]],
];

const COMPONENT_TWO_QUESTIONS = drafts.map(([code, concept, commandWord, prompt, points], index) => {
  const topic = COMPONENT_TWO_TOPICS.find((item) => item.code === code);
  return {
    id: `exam-c2-${code.replaceAll(".", "")}-${index % 2 + 1}`,
    specificationId: "ocr-h446-2020", componentId: "h446-02",
    topicId: topic.id, topicCode: code, topicTitle: topic.title,
    conceptIds: [`${topic.id}:${concept}`],
    objectives: code === "2.1.2" && concept === "document-contract"
      ? ["2.1.2(a)", "2.1.2(b)"] : code === "2.1.3" && concept === "problem-parts"
        ? ["2.1.3(a)", "2.1.3(c)", "2.1.3(d)"] : [...topic.cards.find((card) => card.id === concept).objectives],
    marks: points.length,
    commandWord, prompt, rubric: points.map((description) => ({ description, alternatives: [] })),
    modelReasoning: points.join(" "), responseType: "short_answer", difficulty: 2,
    expectedMinutes: points.length + 1, provenance: "original_neat_notes",
    contentVersion: VERSION, reviewStatus: "review_pending",
  };
});

module.exports = { COMPONENT_TWO_QUESTIONS };
